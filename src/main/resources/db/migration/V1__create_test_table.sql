CREATE TABLE testing_table (
                         id BIGINT PRIMARY KEY,
                         title VARCHAR(255) NOT NULL,
                         content TEXT,
                         published_at TIMESTAMP
);